[package]
name = "lib-utxo"
version = "0.1.0"
edition = "2021"
description = "Thin re-export of canonical UTXO primitives from lib-types"
license = "MIT"

[dependencies]
lib-types = { path = "../lib-types" }
