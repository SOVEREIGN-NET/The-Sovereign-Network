//! Node Type Definition and Detection System
//!
//! Re-exports the canonical `NodeType` from `lib-types` to maintain backward
//! compatibility for existing `crate::config::NodeType` references.

pub use lib_types::NodeType;
